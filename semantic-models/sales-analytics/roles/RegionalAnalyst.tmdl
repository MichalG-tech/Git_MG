role 'Regional Analyst'
	modelPermission: read

	tablePermission DimRegion =
		[RegionName] IN
		    CALCULATETABLE(
		        VALUES(DimRegion[RegionName]),
		        FILTER(
		            DimRegion,
		            DimRegion[RegionID] <= 25
		        )
		    )
